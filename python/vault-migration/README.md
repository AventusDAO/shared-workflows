# INTRO
This project will be used to migrate the gateway ec2 vault data to the new vault EKS clusters.

We need to run it on eks to ensure data integrity since we must not access mainnet split fee pkeys.